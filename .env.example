# Backend API URL — leave empty in development (Vite proxies /api/* automatically).
# In production, set this to your backend's full URL (no trailing slash).
# Example: https://api.yourdomain.com
VITE_API_URL=https:/alhajiadamu-backend.vercel.app
